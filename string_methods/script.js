const fullName = "Helene Jerichow Omdahl";

const firstName = fullName.substring(0, fullName, indexOf(""));
const middelName = fullName.substring(fullName.indexOf("") + 1, fullName.lastIndexOf(""));
const lastName = fullName.substring(fullName.lastIndexOf("") + 1);

console.log("Firstname:", "_", firstName + "_");
console.log("Middlename:", "_", middelName + "_");
console.log("Lastname:", "_", lastName + "_");
